# ============================================================
# Зависимости: Knowledge Base Pipeline (Антигравити Копирайтер)
# ============================================================

# Парсеры файлов
pymupdf>=1.25.0              # PDF (PyMuPDF / fitz)
python-docx>=1.1.0           # DOCX
odfpy>=1.4.1                 # ODT (OpenDocument)
chardet>=5.2.0               # Автодетекция кодировки для TXT
lxml>=5.0.0                  # XML-парсинг (ускорение FB2)

# OpenAI (дистилляция + embeddings)
openai>=1.50.0
httpx>=0.25.0,<0.28.0

# Qdrant (векторная БД)
qdrant-client>=1.12.0

# Утилиты
python-dotenv>=1.0.1
tqdm>=4.67.0
